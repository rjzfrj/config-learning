spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: cloud-config-serivce-eureka
      profile: dev
  application:
    name: foo
    
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://user:password123@localhost:8761/eureka
  instance:
    prefer-ip-address: true
    
    
    
